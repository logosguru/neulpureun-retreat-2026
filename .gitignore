# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# form import 산출물 (개인정보 — 커밋 금지)
scripts/form_import/out/

# 로컬 도구 산출물 (커밋 금지)
.playwright-mcp/
scratchpad/

# 아이스브레이커 Game 03 대상자 개인 사진 (공개 repo — 커밋 금지)
scripts/icebreaker/people/

# 점심 식사 그룹 명단 (성도 실명 192명 — 공개 repo, 커밋 금지)
scripts/lunch-groups/
